pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()

        /*DEV_HELP : Add MAVEN repo dependency from extrieve */
        maven { url 'https://expack.extrieve.in/maven/' }
    }
}
rootProject.name = "SplicerAi"
include ':app'
